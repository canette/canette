{
  "name": "@canette/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --compile --outfile api-server",
    "build:migrate": "bun build scripts/migrate.ts --target=bun --compile --outfile db-migrate",
    "db:migrate": "bun run scripts/migrate.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix --fix-dangerously"
  },
  "dependencies": {
    "@canette/types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-auth": "^1.6.25",
    "hono": "4.12.31",
    "jose": "^6.2.4",
    "js-yaml": "^5.2.2",
    "kysely": "^0.29.4",
    "pg": "^8.22.0",
    "resend": "^6.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.0",
    "bun-types": "^1.3.13",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
