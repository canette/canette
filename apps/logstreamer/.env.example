# Copy to .env and fill in values before running.

# Shared secret for authenticating requests from the API — generate with:
#   openssl rand -hex 32
# Must match LOGSTREAMER_SECRET in the API process.
LOGSTREAMER_SECRET=

# Address and port to listen on.
ADDR=:8080
